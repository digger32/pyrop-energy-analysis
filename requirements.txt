# Pinned runtime requirements for reproducibility.
# Use environment.yml for Conda or pyproject.toml for editable pip installs.

numpy>=1.24,<3.0
pandas>=2.0,<3.0
scipy>=1.11,<2.0
scikit-learn>=1.3,<2.0
statsmodels>=0.14,<1.0
matplotlib>=3.7,<4.0
seaborn>=0.12,<1.0
openpyxl>=3.1,<4.0
xlrd>=2.0,<3.0

# Optional gradient-boosting libraries (the ML benchmark works without them
# but the leaderboard becomes shorter).
xgboost>=2.0,<3.0
catboost>=1.2,<2.0
lightgbm>=4.0,<5.0

# Optional SHAP support for feature-importance explanations.
shap>=0.45,<1.0

# Optional Jupyter support.
jupyterlab>=4.0,<5.0
ipywidgets>=8.1,<9.0
